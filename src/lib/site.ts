/**
 * Название и адрес сайта — одно место на весь проект.
 *
 * До этого одна и та же строка с доменом лежала в трёх файлах (карта сайта, layout,
 * магазин), и при переезде домена любой из них мог остаться со старым значением.
 *
 * Контактов здесь нет намеренно: телефон и ссылки на мессенджеры сейчас захардкожены
 * в MessengerButtons и Footer. Их стоит собрать сюда же, но отдельным заходом —
 * заводить константы «на будущее», которыми никто не пользуется, значит через месяц
 * не знать, какая из двух копий настоящая.
 */

export const SITE_NAME = "Caranalizer";

export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://caranalizer.com";
