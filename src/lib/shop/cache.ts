import 'server-only';

/**
 * Сколько живёт кэш фасетов — общий на обоих доноров.
 *
 * Фасеты (марки, модели, диаметры и их счётчики) меняются ровно тогда, когда прогоняется
 * заливка — `partsfit:load` или `skywheel:load`, — то есть раз в скрап. Часовой срок здесь
 * с большим запасом.
 *
 * Кэшировать их обязательно: фильтр стоит СНАРУЖИ `<Suspense>` (прятать его под скелет
 * на каждый клик хуже, чем подождать), а значит его запрос задерживает оболочку страницы.
 *
 * Константа общая, а не по копии на донора, намеренно: доноров уже двое, и разъехавшиеся
 * сроки жизни кэша дали бы разное поведение одинаковых с виду страниц.
 */
export const FACETS_TTL = 3600;
