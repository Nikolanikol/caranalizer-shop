@AGENTS.md

# Где мы сейчас: каталог partsfit

Раздел запчастей перестроен со скрапа донора partsfit.co.kr и **живёт на проде
с 26 августа 2026**. **28 августа он открыт на международный рынок**: английский язык,
цены в долларах, доставка по миру, опубликованы страницы артикулов. Ниже — состояние
на эту дату, чтобы вернуться к нему без пересбора контекста.

Правила и ловушки — в `AGENTS.md`, устройство скрапера — в `scripts/partsfit/README.md`.
Здесь только «где остановились и что дальше».

## Сделано

**Скрапер написан и отработал.** `scripts/partsfit/`, два прохода: индекс группы по
листингам (48 товаров за запрос), потом карточки — только под то, что публикуем.

Снято и разобрано четыре группы донора:

| группа донора | в индексе | с артикулом | страниц | экземпляров |
|---|---|---|---|---|
| Блоки управления (`cate_no=48`) | 19 188 | 18 838 | 10 933 | 18 830 |
| Фары (`43`) | 13 096 | 7 353 | 3 215 | 7 351 |
| Боковые зеркала (`42`) | 15 773 | 4 609 | 2 521 | 4 608 |
| Фонари (`44`) | 10 555 | 3 935 | 1 986 | 3 935 |
| **Итого** | | | **18 655** | **34 724** |

Страниц больше, чем типов деталей в группе: фонари донора дали ещё противотуманки,
накладки задней панели и обшивки двери багажника — всего в каталоге одиннадцать типов.

44 марки, 362 модели, 209 003 фотографии, 3 587 кросс-номеров, 20 556 экземпляров
с VIN донорской машины.

**Таблица совместимости `partsfit_fitment`** — 16 407 строк на 15 708 артикулов и 359 машин,
из них 591 артикул встречается на нескольких машинах. Пересобирается из `products`
при каждом прогоне `partsfit:tables`, руками не правится. Спорные артикулы отсеиваются:
820 штук, у которых расходится тип детали или сторона.

**Словари закрыты полностью** по всем четырём группам: непокрытых терминов ноль,
из 58 612 заголовков не разобралось 5.

**Таблицы под Supabase собраны** — `data/partsfit/tables/`, семь файлов, поля совпадают
с `supabase/migrations/`. Проверено: детерминизм (одинаковые хеши двух прогонов),
соответствие схеме, ссылочная целостность, уникальность адресов.

**План выполнен целиком: 26.08.2026 база и код на проде.** Миграции через SQL Editor,
313 925 строк залиты, проверка 35 из 35, код в `main` (коммит `2675aa2`), деплой прошёл.

Проверено на живом сайте, а не только в сборке: на 26.08 `caranalizer.com/sitemap.xml`
отдавал **8 024 адреса** — 5 834 карточки, 1 851 модель, 309 марок, 14 категорий.
Сходилось с манифестом сборки до штуки. С 28.08 их 47 461 — см. ниже.

**С 28.08.2026 адресов 47 461**: раздел открыт на английском, в карте по записи
на каждый язык с полным `hreflang`, и опубликованы страницы артикулов — 31 416 из них,
по 15 708 на язык. Русских адресов не потеряно ни одного, сверено с прежней картой.
Это 95% от лимита Google в 50 000 на файл; делить карту нечем, подробности в `AGENTS.md`.
**После выкатки карту надо переотправить в Search Console** — сама Google узнает нескоро.

**Цена на витрине только в долларах** (с 28.08.2026). Рубль убран отовсюду, где его
видит покупатель: раздел работает на международный рынок. Рублёвый расчёт остался
в заявке менеджеру справочным, со знаком «≈».

**Главная стала входом в каталог.** Герой про запчасти с поиском по артикулу, под ним
типы деталей, свежие поступления и марки. Проверка по VIN осталась блоками ниже —
главная её единственная органическая площадка.

**С 28.08.2026 главная есть и на английском**, как и весь сайт: `/en` больше не уводит
на страницу проверки. Английские тексты у гайдов, «о нас», FAQ и «как мы работаем»
лежали в коде всё это время — их прятал редирект.

**Страница артикула `/zapchasti/oem/<номер>` работает (шаг 4).** Показывает, на каких
машинах встречается номер, и что есть в наличии — по основному номеру и по кросс-номерам.
Артикул на карточке товара ведёт на неё. Закрыта `noindex` и не стоит в карте сайта:
публикация отложена до расчистки индекса, снять — один флаг в `generateMetadata`
и одна строка в `sitemap.ts`.

**Карточка со списком экземпляров работает (шаг 3).** На детали
с 64 экземплярами покупатель выбирает конкретный: цена, состояние, фотографии, VIN
и разъём переключаются вместе. В корзину уезжает выбранный экземпляр, а не деталь.
У зеркал в списке виден код цвета кузова.

**Витрина переехала на базу (шаг 1 плана сделан).** `src/lib/shop/catalog.ts` читает
Supabase, `src/types/part.ts` разводит деталь и экземпляр, категорий в реестре
одиннадцать. Старые `catalog.json`, `raw_parts.json` и `normalize-parts.mjs` удалены.
Сборка: **5 860 страниц за 2,5–5 минут** (разброс от прогретости кэша), из них
5 834 карточки. Число не изменилось и после открытия английского: иноязычные страницы
рендерятся по первому запросу. Карта сайта — 47 461 адрес.

**База поднята локально и проверена целиком.** Supabase CLI, Postgres **15.8** — та же
версия, что на VPS (`major_version` в `config.toml` под неё и выставлен). Схема живёт
в `supabase/migrations/` (три миграции: каталог, совместимость, представления счётчиков),
применяется `supabase db reset` с нуля. 313 925 строк заливаются за ~11 секунд,
все 24 проверки зелёные.

## Открытие на международный рынок — 28.08.2026

Повод — не догадка, а выгрузка Search Console: **половина поисковых кликов приходила
на `/en/` (29%) и `/ar/` (21%)**, русскоязычный мир дал 40,7%, остальной 59,3%. Посылка
«иноязычный покупатель обслуживается на kmotors» оказалась неверной дважды: kmotors
торгует **новыми** деталями, а английские тексты у наших страниц лежали в коде всё это
время — их прятал редирект `/en → проверка по VIN`. Цифры и разбор — в памяти,
`parts-demand-is-oem-numbers`.

**Сайт открыт на английском целиком.** Раздел, главная, гайды, «о нас», FAQ. Словарь
каталога — 60 терминов в `lib/shop/terms.ts`, полнота сторожится `npm run partsfit:terms`.
Переводить пришлось мало: свободной прозы в данных нет, заголовок собирается из типа
детали, марки, модели и года. `ar` остался свёрнутым — раздел на нём не открыт.

**Цена оферты — доллар, рубля на витрине нет.** «≈» из формата убран: приблизительной
цены в оферте быть не может. Рублёвый расчёт остался только в заявке менеджеру,
справочным.

**Страницы артикулов опубликованы** — 15 708 из 16 755. Остальные 1 047 закрыты
`noindex`: у них нет строки в `partsfit_fitment`, и страница держится только на товаре.
Ждать расчистки индекса решили не бесконечно — эти страницы самые ценные по трафику.

**Карта сайта: 8 024 → 47 461 адрес**, по записи на язык с полным `hreflang`.
Это 95% от лимита Google в 50 000 на файл; делить нечем, подробности в `AGENTS.md`.

**Форма заявки снята с формы kmotors.** Пять полей: имя, телефон с выбором страны,
«как вам ответить», страна доставки, VIN. Российского контекста не осталось —
ни списка городов, ни адреса ПВЗ СДЭК, ни выбора способа оплаты.

**Оферта применена ко всем странам.** Сроки доставки по миру называет менеджер;
таблица по городам осталась только в русской версии, где оценки настоящие.

**Заявки помечены `#caranalizer`** — чат общий с kmotors, метку ставит `submitLead`.

**Воронка раздела размечена** — `view_item`, `add_to_cart`, `begin_checkout`,
`generate_lead`. Без цены: решение владельца, в отчётах нужны штуки.

**Подделка суммы заказа закрыта.** Сервер дочитывает воны из базы по ключу позиции
и считает сам; присланное идёт только на сверку, расхождение попадает в заявку строкой.

## Регистрация покупателей — на проде с 30.08.2026

Повод: **почты покупателя у нас не было нигде.** Заявка из корзины приносит имя, телефон,
страну и VIN; форма проверки — имя и телефон. Написать человеку о поступлении детали
было нечем, и весь накопленный спрос жил ровно до конца одного разговора в мессенджере.

**Вход и регистрация работают: Google и почта с паролем.** Страницы `/<язык>/auth`
и `/<язык>/account`, обе двуязычные и обе `noindex`. В шапке — «Войти», у вошедшего
«Кабинет». Провайдер входа стоит в `[lang]/layout.tsx` выше шапки.

**Пул аккаунтов общий с kmotors — решение владельца.** Вход идёт в тот же GoTrue боевой
базы, где у kmotors уже работают свои `/auth` и `/account`. Клиент kmotors войдёт к нам
своим аккаунтом и наоборот; площадки партнёрские, владелец один. Google в этом GoTrue
**уже включён** — в Google Cloud Console заводить нечего.

**Покупатели лежат в своей таблице `partsfit_customers`**, а не в `profiles` kmotors:
почта, имя, телефон, язык, способ входа, площадка первой встречи и согласие на рассылку.
Пишет её только сервер (`/api/auth/sync`) и только после проверки токена — почта берётся
из подтверждённого токена, а не из тела запроса.

**Сессия живёт в localStorage, а не в cookie.** Это не стилистика: cookie пришлось бы
читать на сервере, а обращение к запросу в layout переводит в динамику весь сайт.
Сборка после регистрации осталась прежней — ни один маршрут статики не потерял.

**Проверено локально целиком, на продовом поведении.** Локальный GoTrue переведён
в тот же режим, что прод (подтверждение почты обязательно), и путь пройден весь:
регистрация → письмо в Mailpit (`127.0.0.1:54324`) → ссылка → сессия → кабинет с именем
и телефоном, которые ждали в метаданных пользователя всё время между регистрацией
и подтверждением. Плюс вход паролем, выход, сторож кабинета и отказ API без токена (401).
**Google тоже проверен локально** (29.08.2026): в клиенте Google разрешён локальный
адрес возврата рядом с боевым, вход прошёл, строка в `partsfit_customers` легла
с `provider = google`, именем из аккаунта и пустым телефоном — Google его не отдаёт.
Клиент у нас с kmotors один и тот же, это выяснилось из публичного редиректа боевого
GoTrue. Команды и ловушки — в `AGENTS.md`, раздел «Как проверить вход локально».

### Что пришлось настроить на сервере — 30.08.2026

Код был готов накануне, а день ушёл на окружение. Ни одна из пяти находок не была видна
до выкатки, и каждая ломала регистрацию молча.

**1. Миграция** `20260829140000_partsfit_customers.sql` через SQL Editor — до деплоя кода.
Сделана. Плюс отдельной строкой `revoke all … from anon`: прод раздаёт новым таблицам
права анонимному ключу сам, в отличие от локального стека (подробности в `AGENTS.md`).

**2. Список разрешённых адресов возврата у GoTrue.** Был **пуст**, а `SITE_URL` — 
`https://www.kmotors.shop`. То есть любой возврат на наш домен GoTrue подменил бы
на kmotors, и это не выглядело бы ошибкой. Задаётся не там, где ищешь: в self-hosted
разделов URL Configuration и OAuth Apps в Studio нет вовсе, а в compose переменная
собирается из **`ADDITIONAL_REDIRECT_URLS`** — задать `GOTRUE_URI_ALLOW_LIST` напрямую
бесполезно, compose её перекроет. Сейчас там наши два домена и kmotors.

**3. Почта не была настроена никогда** — ни у нас, ни у kmotors: все `SMTP_*` пустые.
Подключён Resend, отправитель `noreply@kmotors.shop`, домен подтверждён в Resend,
DMARC добавлен в Cloudflare рядом с SPF и DKIM.

**Порт — 2587, и это не вкусовщина.** Хостинг режет исходящие 25, 465 и 587: GoTrue
висел на них ровно десять секунд и отваливался по таймауту без внятной ошибки.
У Resend для таких случаев есть 2465 и 2587; берём 2587, потому что на нём шифрование
включается после подключения (STARTTLS), а GoTrue именно так и делает для всех портов,
кроме 465.

**4. Свои письма** — `public/email/confirm.html` и `recovery.html`, по-английски
(решение владельца: письма общие на две площадки). Стандартный шаблон GoTrue отправился
в спам с первого же письма. Переменные `MAILER_TEMPLATES_*` и `MAILER_SUBJECTS_*`,
та же ловушка с именами, что в пункте 2.

**5. `SUPABASE_SERVICE_ROLE_KEY` на проде не был задан вовсе.** Сайт всё это время
работал анонимным ключом, и это годами не проявлялось: каталог анониму открыт, `leads` —
таблица общая с kmotors — тоже. Сломалось на первой нашей таблице, закрытой от анонима:
кабинет отвечал «не удалось сохранить», причина не была видна ниоткуда. Теперь ключ
задан, а роут `/api/auth/sync` проверяет его явно и называет переменную в ответе.

Про Google на проде: провайдер там включён давно, и клиент **тот же**, что у kmotors —
выяснилось из публичного редиректа их GoTrue. В Google Console пришлось добавить лишь
локальный адрес возврата для тестов; боевой `https://api.kmotors.shop/auth/v1/callback`
там был всегда, и наш домен Google не видит вовсе.

### Код из письма — второй путь подтверждения

Письмо GoTrue всегда предлагает шестизначный код рядом со ссылкой, а ввести его было
негде. Дыра при этом настоящая: **ссылка работает только в том браузере, где начали
регистрацию** — при PKCE секрет остаётся в его хранилище. Владелец на это и наступил,
открыв письмо в другом профиле Chrome. Экран «письмо отправлено» теперь принимает код,
и он проверяется на сервере, то есть работает с любого устройства.

### Сколько человек зарегистрировалось — и во что обошёлся молчавший почтовик

Замер 01.09.2026 по боевой базе. **В `partsfit_customers` четыре строки**: одна почтой
(29.08, тестовая), три через Google (30.08). Все с площадки `caranalizer`, язык `ru`.
**Согласие на рассылку не дал никто — 0 из 4:** у входа через Google галочку ставить негде,
и `marketing_ok` остаётся `false`. То есть почта копится, а права написать нет.

Потерь при этом нет: все четыре аккаунта, заведённых с 29.08, имеют строку у нас. Один
вход без строки был — 29.08 в 15:08, аккаунт владельца, заведённый 10.06 на kmotors;
это тот самый незаданный `SUPABASE_SERVICE_ROLE_KEY`, а не клиент.

**Дороже оказалась находка по общему пулу GoTrue** (21 аккаунт):

| путь регистрации | всего | подтвердили почту | входили |
|---|---|---|---|
| Google | 5 | 5 | 5 |
| почтой, до 29.08.2026 | 13 | **0** | **0** |
| почтой, с 29.08.2026 | 3 | 3 | 3 |

Тринадцать человек между 10.06 и 16.08.2026 оставили почту и застряли: SMTP не был
настроен ни у нас, ни у kmotors, письма с подтверждением не уходили никуда. Это **потери
kmotors** — наша регистрация появилась только 29.08, — и видны они лишь потому, что пул
общий. Адреса живы в `auth.users`; переслать им подтверждение технически можно, но это
рассылка без согласия, и делать её надо из kmotors.

### Уведомления о регистрации в Telegram — сделано 01.09.2026

Прямое следствие находки выше: слепота стоила тринадцати адресов, и закрывается она
уведомлением в момент **начала** регистрации, а не её завершения.

Три сообщения в рабочий чат (`TELEGRAM_WORK_CHAT_ID`, решение владельца — второй чат общий
с kmotors): `📝 Начал регистрацию почтой` до подтверждения, `⚠️ Письмо НЕ отправлено`
тревогой о почтовике и `✅ Зарегистрировался и вошёл` в конце пути. Google даёт только
третье — письма там нет вовсе. В `leads` регистрация не пишется: она не заявка, и строка
там сломала бы сходимость `lead_source` с `source_page`.

Новых переменных окружения не нужно, миграций тоже — деплой в один шаг.

Проверено локально целиком на продовом поведении (подтверждение почты включено):
регистрация → уведомление → письмо в Mailpit → подтверждение → вход → второе уведомление.
Плюс сторож роута: пять поддельных запросов подряд не отправили ничего и вернули тот же
`202`, что и настоящий. Сборка — 5 982 маршрута, без изменений.

Устройство, ловушки и порядок локальной проверки (заглушки `TELEGRAM_*` в `.env.local`,
без них dev пишет в настоящий чат) — в `AGENTS.md`.

## Декодер VIN — на проде-кандидате с 30.08.2026

Страница проверки была одной большой формой заявки: посетитель не получал от нас
ничего, пока не оставит телефон. Теперь марку, страну завода и год он видит сразу,
как только наберёт номер.

**Два слоя, и они разной природы.** Базовый — марка, страна, год — считается
**в браузере**, без единого запроса: результат появляется по мере ввода семнадцатого
знака. Расширенный — корейский реестр снятых с учёта на экспорт и база NHTSA — только
для вошедших. Гейт держит роут `/api/vin`, а не разметка: сессия в localStorage, и
спрятанный версткой блок отдал бы данные всем, кто откроет вкладку сети.

**Гейт на вход — решение владельца 30.08.2026.** Довод: почты покупателя нет нигде,
кроме регистрации, а до VIN доходят самые заинтересованные. Событие `vin_decode`
шлётся со значением `locked`, когда человек упёрся в замок, — по нему и будет видно,
окупается гейт или отпугивает.

**Блок переделан 30.08.2026 по замечанию владельца.** Первая версия называлась
«Расшифровка VIN» и обещала самый дешёвый слой — марку, страну, год. Теперь заголовок
обещает то, ради чего проверку и заказывают: снята ли машина с учёта на экспорт и с каким
пробегом её сдали. Пробег вынесен ведущим полем. Блок сделан заметным (рамка акцентным
цветом, заголовок 30 против 18, поле 56 против 44, янтарная кнопка) — прежний владелец
просто не замечал на странице. Добавлены история проверенных номеров в localStorage
и кнопка копирования отчёта.

**Логика выделена из папки `вин-дешифратор/`** (апплет AI Studio, добавлен в корень).
Взяты разбор и тесты; папка при этом **ломала сборку** — включалась по `**/*.ts`
и давала 9 ошибок типов. Исключена в `tsconfig.json`, удалить можно целиком.

Что чинилось при выделении: год считался только по циклу 2010–2039, и машина 2008 года
выходила 2038-й; три куска мёртвого кода (карта года с ключами-пробелами, двухзначные
WMI, недостижимые при поиске по трём знакам, пересоздание таблиц на каждый вызов);
английское «Unknown» в слое данных на трёхъязычной странице.

**Проверено на живом сервере:** мгновенный разбор (Hyundai · Южная Корея · 2023 г.
без запроса), полный путь под вошедшим (реестр ответил, NHTSA вернула завод ULSAN),
отказ анониму и по подделанному токену, границы валидации, все три языка.
Сборка — 5 982 маршрута, страница осталась статикой.

**Ветка «запись найдена» проверена 30.08.2026** на живом номере `WAUZZZ8T8FA028385`
(Audi A5 Sportback 35 TDI quattro, владелец дал): реестр вернул модель, цвет, пробег
143 351 км, топливо, объём и дату первой регистрации. Заодно этот номер подтвердил
способ расчёта года — мы посчитали 2015, реестр показал первую регистрацию 2015-02-03,
а NHTSA сказала 1985.

Он же вскрыл три дефекта, все закрыты в тот же день и покрыты тестами: прочерк реестра
уезжал на витрину значением; `143,351` км читалось по-русски как сто сорок три
километра; цвет и топливо показывались по-корейски (`흰색`, `경유`).

## Источники данных по машине — разведка 31.08.2026, дополнена 01.09.2026

Повод: первые семь живых проверок по VIN вернули `not-found` **все семь**. Вопрос был
поставлен так: это мы сломались или номера странные. Ответ — ни то ни другое, а по
дороге выяснилось, где вообще лежат данные, за которые платят.

**Наш путь исправен.** Контрольный номер `WAUZZZ8T8FA028385` прогнан через настоящий
`fromCar365` — реестр вернул полную запись (A5 Sportback, пробег 143 351 км, первая
регистрация 2015-02-03). Значит `not-found` у семи проверок — честный ответ портала,
а не молчаливая поломка регулярки. Проверять так и дальше: у нас есть номер, который
обязан находиться, и расхождение с ним — единственный надёжный признак поломки.

**Пусто было потому, что реестр узкий.** В нём только машины, снятые с учёта на экспорт
из Кореи, а вводили посетители свои текущие: Kia американской сборки, Land Rover,
Hyundai 2006 года и два Audi. Одна из семи — исправленный вариант предыдущей: человек
не мог разобрать собственный VIN.

### Таблица заводов пополнена — 12 кодов

Номер `5XXGT4L31LG441304` (Kia Optima сборки Джорджии) не разобрался вовсе: **кодов
заводов Hyundai и Kia вне Кореи в таблице не было**, хотя это ровно наша аудитория.
Добавлены `5XX`/`5XY` (Kia Georgia), `5NP`/`5NM` (Hyundai Alabama), `3KM`/`3KP`
(Kia Mexico) и корейские `KMF`, `KMT`, `KMU`, `KPH`, `KL1`, `KL8`.

Все двенадцать сверены со справочником NHTSA (`vpic.nhtsa.dot.gov/api/vehicles/DecodeWMI/<код>`)
до внесения — в поле `make` лежит то, что увидит покупатель, и марка невпопад хуже
пустого места. Тесты 52 из 52, новых стран не появилось.

### Где данные лежат на самом деле

| источник | ключ поиска | что даёт | условия |
|---|---|---|---|
| car365, реестр экспорта | VIN | пробег, снятие с учёта | **открыт**, уже подключён |
| `금융위원회` 15124895 | госномер | залив: дата и тип | **бесплатно, лицензия чистая** |
| Encar `record/.../open` | ID объявления | полная страховая история | открыт, но их внутренний API |
| `국토교통부` 15071233 | номер + имя владельца | история, техосмотр | только с согласия владельца |
| TS, `매매용 차량 이력 API` | — | история по машинам в продаже | по договору, см. ниже |
| `보험개발원` (Carhistory) | **VIN** | страховая история целиком | платно: 990–2 200 ₩ за запрос |

### car365: открыта только та дверь, что уже открыта

Запрос по истории — один эндпоинт `unityhstry/vhrnoView.do` с кодом области:
`01` своя машина, `02` чужая с согласия, `03` чужая без согласия, `04` **машина на продаже**.
Все четыре уводят на вход по корейской идентификации личности. Туда же — `침수정보 조회`.

Ловушка: страница `매매용차량신속조회` (`schdcar/trde/schdTrdeVhclView.do`) **открывается
без входа, код 200** — и потому выглядит доступной. Её данные не доступны:
`unityHstryValid.do` отвечает 500 с `로그인 정보가 존재하지 않습니다`, `getAsocTrdeCar.do`
— 403 `접근 권한이 존재하지 않습니다`. Проверено на живом номере.

Из кода той страницы видно, что было бы в ответе: модель, класс, `usgSeCd` (вид
использования — там всплывает такси и прокат), дата первой регистрации, топливо,
`drvngDstnc` (пробег) и `btnUrl` — ссылка на полную запись ассоциации.

### data.go.kr: бесплатно всерьёз, но почти всё — статистика

Портал раздаёт данные по-настоящему: `비용부과유무: 무료`, лицензия часто без ограничений,
согласование автоматическое. Но **семейство `종합정보` от Управления транспортной
безопасности — сводки, а не запросы по машине**, и названия об этом не говорят.
Пойманы три подряд: `정비이력정보` (15057856), `검사정보` (15057736), `성능점검정보`
(15057532) — на входе год, месяц, класс, код региона, на выходе статистика за период.
`자동차보험가입정보` (15124891) от Комиссии по финансовым услугам — то же самое:
сколько полисов, какая премия, убытки по классам. Проверять описание, а не название.

**Годится ровно один набор: `금융위원회_침수차량진위확인` (15124895).** Ищет по госномеру,
отдаёт дату и тип происшествия с заливом. Бесплатно, лицензия без ограничений,
согласование автоматическое **и на разработке, и в эксплуатации** (редкость — у большинства
боевой доступ через рассмотрение), 10 000 запросов в сутки.

Заявка — кнопка `활용신청` на `data.go.kr/data/15124895/openapi.do`, ключ потом лежит
в `data.go.kr/iim/api/selectAPIAcountView.do`. **Ключей выдают два, `Encoding`
и `Decoding`, и это один ключ в двух видах:** собираете адрес строкой — берите первый,
через `URLSearchParams` (как в `sources.ts`) — второй, иначе он закодируется дважды
и сервис ответит «ключ неверный» при верном ключе.

Почему страховой истории там нет и не будет: у `보험개발원` на портале три набора, и все
про медицинское страхование. История машины — их товар, он называется Carhistory
и продаётся.

### Encar публикует страховую историю сам

`api.encar.com/v1/readside/record/vehicle/<id>/open` отдаёт **без авторизации** тот самый
набор от `보험개발원`: число ДТП по своей и по чужой страховке, разбивку каждого случая
на детали / работу / покраску, полную гибель, залив (полный и частичный), угон,
такси / прокат / госслужбу, смены владельца и номера, **периоды без страховки**.
Проверено на объявлении 42203405 (Kia K7 2017, 301 126 км): три ДТП, ремонта
на 1 876 343 ₩, и семь месяцев без полиса сразу после смены владельца.

`api.encar.com/v1/readside/vehicle/<id>` отдаёт **VIN** — то есть объявления соединяются
и с нашей проверкой, и с каталогом запчастей по одному ключу.

Три оговорки, без которых на это опираться нельзя: у объявления стоит флаг `openData`,
и **какая доля продавцов раскрытие включает — не измерено**; это внутренний API Encar
без опубликованных условий для программного доступа; трубу закроют — узнаем постфактум.

### Откуда берёт данные HeyDealer

Из их же политики обработки данных, раздел «제공 내역» — корейский закон обязывает
называть получателей поимённо:

| получатель | зачем |
|---|---|
| `보험개발원` | **보험이력 조회** — страховая история |
| `정부24` + `(주)헥토데이터` | `자동차등록원부` — регистрационная карточка |
| `국토교통부` | данные министерства |
| `사단법인 온라인내차팔기협회` | обязательные данные для онлайн-продажи |

Соглашение, ст. 11-2: данные собраны «적법하게» из공공데이터.

Та же ассоциация `온라인내차팔기협회` стоит за эндпоинтом `getAsocTrdeCar.do` на car365
(в коде страницы комментарий `매매용 차량 협회조회`) — это один и тот же источник.

**Прежняя запись здесь утверждала, что всё держится на согласии собственника и обходного
пути нет ни у кого. Это неверно** (проверено 01.09.2026): согласие нужно трём источникам
из четырёх, а страховая история — исключение, см. ниже.

### Что видно в выдаче HeyDealer — снято с их сервера 01.09.2026

Серверный рендер карточки объявления (`/market/cars/<hash>`) отдаёт всю модель данных
целиком, без авторизации. На живом Grandeur IG 2019:

```
max_power "190 마력"  efficiency  displacement  car_length/width  factory_price
    ↑ лежат в одном блоке с grade_hash_id — это справочник комплектаций
initial_registration_date "2019-11-22"      year   mileage
total_info { plant "충남 아산 공장 🇰🇷", records_count 9, owner_changed_count 1 }
carhistory {
  my_car_accident_list [{ accident_date, component, painting, wage, wage_description,
                          insurance_money, insurance_amount, source "repair_estimate" }],
  other_car_accident_list, owner_changed_list, car_number_changed_count,
  total_loss_count, flooded_count, stolen_count, has_rent_use_record }
```

Два вывода, и оба меняют планы:

- **Мощность — не факт о машине, а атрибут комплектации.** `max_power` стоит рядом
  с `grade_hash_id`, ценой нового и габаритами. Ни один корейский реестр лошадиных сил
  по номеру не отдаёт: 등록원부 знает объём, не мощность. Купить это негде — нужен
  справочник «модель → трим → характеристики», и это отдельная работа, а не подключение
  API. Дешёвый вход: у Hyundai бесплатная проверка по 차대번호
  (`hyundai.com/kr/ko/service-membership/check-specifications`), машины с выпуска после
  января 2010, отдаёт название модели и цену. Программно не проверено.
- **Даты производства они не показывают вовсе.** На витрине — первая регистрация
  и завод сборки. Настоящее `제작연월일` лежит только в `자동차등록원부`, за ним они и ходят
  в `정부24` через `헥토데이터`. Первую регистрацию мы уже получаем бесплатно из car365
  (`frstRegYmd`) — то есть ради даты платить не за что.

Детализация ДТП у них глубже стандартного отчёта Carhistory: `wage_description` перечисляет
конкретные работы («리어범퍼어셈블리(오버홀)»), а `source: "repair_estimate"` намекает
на систему расчёта ремонта, а не на сводку. Это уровень их договора, не публичный.

### Цены 보험개발원 и главное: вход по VIN

Дословно со страницы `carhistory.or.kr/partner/alliance.car` и формы поиска:

| способ | условия |
|---|---|
| разовый запрос, гость | **2 200 ₩** за штуку |
| разовый, с `본인확인` | 770 ₩ за первые 5 в год, дальше 2 200 ₩ |
| **제휴, сервер-сервер** | договор от **1 000 запросов в месяц**, сверх — **990 ₩** за штуку |

Бесплатны ровно две вещи: `무료폐차사고조회` и `무료침수사고조회`. Остальное платно, и они
сами объясняют почему — «수익자부담원칙», платит выгодоприобретатель.

**Форма ищет по 차량번호 ИЛИ 차대번호.** Это и есть находка: ключ, который у нас есть,
подходит, а госномер и имя владельца — из-за которых мы упирались во все остальные
корейские источники — здесь не нужны вовсе. Причина в природе данных: это страховая
статистика по машине, а не персональные данные владельца.

**Второе: 보험개발원 сам продаёт это за границу под экспортные машины.** С октября 2023 —
CARSEER (ОАЭ), с августа 2025 — carVertical (Европа и Африка). Сайт переключается
на русский, арабский, монгольский, вьетнамский. Наш случай — ровно тот, под который
это и открывали.

Вход: заявка только онлайн, 1–2 месяца (документы → рассмотрение → договор → регистрация
IP и тесты → запуск), на тестах дают 5 контрольных номеров. Минимум по договору
≈ 990 000 ₩/мес ≈ 700 $.

### Почему у HeyDealer 10 бесплатных проверок в день

Вопрос владельца был «не может быть, что они получают это бесплатно». Не получают:
по прайсу 10 проверок — порядка 10 000 ₩ себестоимости на пользователя в день.
Окупается тремя вещами сразу:

- они **лицензированный `자동차매매업자`** (ссылка `자동차매매업정보확인` у них в подвале);
- **`자동차관리법` ст. 58** обязывает продавца до сделки письменно раскрыть покупателю
  историю и `성능·상태점검기록부` — то есть по машинам в продаже эти данные они всё равно
  обязаны купить, и проверка чужого номера идёт поверх уже оплаченной базы;
- аукцион на 40 000+ машин в месяц: LTV одного продавца несопоставим с ценой запроса.

Что часть выдачи идёт из их собственного накопленного кэша — предположение, не факт.

### Бесплатное у государства есть, но упирается в госномер

- **`국토교통부` 15071233 на data.go.kr — бесплатный** («비용부과유무: 무료»), и объём
  приличный: `기본정보` 32 поля, `제원` 70, `정비이력` 16, `성능점검` 56. Вход —
  `차량번호` + `소유주명` + согласие владельца. Согласование: разработка автоматом,
  эксплуатация через рассмотрение.
- **`첨부형 API` от 한국교통안전공단** — до **90 ₩** за вызов, часть полей бесплатна.
- **`매매용/사업용 이력 API`** — те самые поля «без согласия владельца», по договору.

Порядок действий отсюда: сперва звонок в `한국교통안전공단` (бесплатно или 90 ₩ за вызов),
и только если откажут — заявка на `제휴` в `보험개발원`. HYPHEN в этой картине лишний:
он перепродаёт то же самое дороже.

### Посредники и цены

`헥토데이터` торгует под маркой **HYPHEN** (`hyphen.im`), продукт `자동차 종합정보 조회`:
история ДТП, страховые выплаты, залив, разбор VIN, отзывы, тюнинг, смены владельца.
`등록원부` и `성능점검` в него не входят — это отдельные продукты. Доставка — скрапинг,
то есть юридический вопрос не исчезает, а переносится на подрядчика.

| план | абонплата | как считают |
|---|---|---|
| TR 슬림 | 100 000 ₩/мес | предоплата «бизмани», списание за ответ |
| TR 시그니처 | от 300 000 ₩/мес | пакет обращений, сверх — постоплата |

**Цену за запрос не публикует никто на этом рынке** — ни HYPHEN, ни CODEF (`codef.io`,
калькулятор тарифов закрыли), ни `데이터허브` (`dataapi.co.kr`). Цифру дают по обращению:
`help@hyphen.im`, 1600-4173 (2), будни 08:30–17:30 KST.

### Официальный сервис под машины на продаже — есть

Объявление 한국교통안전공단 об открытии `자동차종합정보` перечисляет три сервиса, и третий:

> **매매용 차량 이력 API** — 자동차 이력정보 중 **소유주 미동의 항목**

История по машинам в продаже, по тем полям, **где согласия владельца нет**. На data.go.kr
как открытый набор он не выложен — доступ по договору. Ответственный указан в объявлении:
отдел информации об автомобилях, `손교정 과장`, **054-459-7492**.

Это первоисточник, а не посредник, и сделан он ровно под наш случай. Звонить туда стоит
раньше, чем писать в HYPHEN: если сервис нам доступен, посредник не нужен вовсе.

## Отклик фильтра на клик — сделано 02.09.2026

Клик по фильтру не показывал ничего: выдача оставалась прежней весь ответ сервера,
а на медленной сети интерфейс выглядел зависшим. Причина не в скорости базы.

**`loading.tsx` не срабатывает, когда меняются только параметры запроса.** Замерено
на чистом Next 16.2.9, а не выведено из документации: смена сегмента пути даёт скелет
сразу, смена `?brand=` — не даёт ни разу, экран стоит все 1 540 мс. А на витрине
параметрами живёт всё: сторона, расположение, сортировка, страница, марка и модель.
Вторая причина — стримить было нечего: `CatalogView` дожидался `findParts` до того,
как вернуть разметку, то есть страница целиком ждала базу.

Переход теперь закрыт с двух сторон, и порознь эти половины дырявые:

- **От клика до смены адреса — `LinkPending`** (`components/shop/link-pending.tsx`),
  обёртка над `useLinkStatus`. Стоит внутри ссылок фильтра, сортировки, пагинации
  и сброса. Сети не требует вовсе: `pending` встаёт через ~9 мс после клика.
- **От смены адреса до прихода товаров — `<Suspense key>`** в `catalog-view.tsx`.
  Выдача выделена в `CatalogResults`, оболочка (шапка, фильтр, поиск, ряд сортировки)
  уходит к посетителю сразу, на месте товаров — скелет.

**Ключ у границы обязателен**, замерено: без него fallback не показывается вовсе,
а коммит адреса ждёт полного ответа (1 540 мс застывшего экрана); с ключом адрес
и скелет коммитятся за 65 мс.

**Фасеты кэшированы** — `getBrands`, `getModels`, `getTopModels` через `unstable_cache`
на час. Это не оптимизация впрок: они рисуют фильтр, а фильтр стоит снаружи Suspense
(прятать его под скелет на каждый клик хуже, чем подождать), то есть их запрос
задерживает оболочку. От фильтра они не зависят и меняются раз в скрап.

**Скелет карточки повторяет `ProductCard` до пикселя** — 631, замерено в браузере.
Первая версия была вдвое ниже, страница на время загрузки складывалась
с 15 624 до 11 356 пикселей, браузер подтягивал прокрутку, и приезд товаров читался
рывком. Карточек в скелете столько же, сколько на полной странице выдачи (`ITEMS_PER_PAGE`),
а не шесть, как в `loading.tsx`: там подменяется чужой маршрут, здесь — та же страница.
Сетка вынесена в `components/shop/skeletons.tsx` и используется обоими.

Проверено на продовой сборке: клик → индикатор на 9 мс → адрес и скелет на 49 мс →
товары. При этом задержки в CSS (80 мс у заливки, 250 мс у полосы) так подобраны,
что на живой сети индикатор не успевает показаться вовсе — говорит скелет. Медленный
ответ проверен отдельно, искусственной задержкой в 1,5 с: индикатор держится весь
`pending`, потом его сменяет скелет. Мобильный случай тоже проверен — раскрытая панель
фильтров переживает переход и не схлопывается.

**Диски сделаны тем же слоем, а не копией** (02.09.2026, по указанию владельца: «базы
могут быть разные, но бэк и фронт универсально принимают и обрабатывают всех»). Панель
над выдачей, скелет и подпись для скринридера живут в `components/shop/results.tsx`
и общие на обоих доноров; индикатор клика — общий `link-pending.tsx`; срок жизни кэша
фасетов — общая константа в `lib/shop/cache.ts`. Карточки различаются ровно одним:
коробка характеристик 42 пикселя у детали против 58 у диска, и это единственный параметр
скелета. `getWheelFacets` кэширован так же, как фасеты каталога.

Проверено на обоих донорах после общей сборки:

| | запчасти | диски |
|---|---|---|
| индикатор клика | 7 мс | 23 мс |
| адрес сменился, встал скелет | 45 мс | 65 мс |
| высота карточки скелета / настоящей | 631 / 631 | 647 / 647 |
| карточек в скелете | 15 | 15 |

Сборка: **5 982 маршрута, из них 5 834 карточки — не изменилось**, листинги остались `ƒ`.

## Не сделано

- **Деплой в Coolify идёт ~10 минут — разобрано 02.09.2026, не сделано.**
  Локальная сборка при этом 105 секунд (`.next/trace-build` от 01.09): генерация статики
  55 с, очистка `.next` 25 с, копирование standalone 17 с, Turbopack 3,7 с, TypeScript 3,6 с.
  Значит дело не в компиляции.

  **База деплой НЕ тормозит** — проверено: `api.kmotors.shop` идёт не через Cloudflare
  (DNS-only на `94.16.116.251`, отвечает Kong с `x-kong-response-latency: 1ms`), внутри VPS
  запрос стоит единицы миллисекунд. Задержка в 300–900 мс — это сборка с ноутбука в прод,
  другая история. Кстати, в `.next/cache/fetch-cache` лежат оба хоста: часть сборок уходила
  в боевую базу. Там же видно цену сборки в запросах — 62 315 записей тремя порциями
  примерно по 20 800, то есть **~20 800 обращений в Supabase за сборку**, 3,5 на карточку.

  Дорого другое — то, что сборка производит:
  `.next/server/app` весит **2,8 ГБ**, из них 1,65 ГБ это 71 368 файлов `.rsc` (Next 16 пишет
  посегментные payload'ы префетча, по 12 на страницу; флага отключения в 16.2.9 в схеме
  конфига нет) и 944 МБ — 5 973 `.html`. `.next/standalone` — полная копия тех же 2,8 ГБ.
  В `Dockerfile` это уезжает строкой `COPY --chown=nextjs:nodejs`, то есть Docker переписывает
  владельца у 78 тысяч файлов одним слоем. Плюс `.next/cache` между деплоями не переносится
  вовсе: Turbopack компилирует с нуля каждый раз, fetch-кэш пустой.

  Что делать, по убыванию эффекта:
  1. **Развести два порога.** `getIndexableParts()` сейчас кормит и `generateStaticParams`,
     и карту сайта (`sitemap.ts:114` и `[slug]/page.tsx:55` зовут одну функцию), поэтому
     поднять порог в лоб нельзя — вместе со сборкой похудеет карта сайта, а это решение
     про SEO. «В индексе» оставить 2 экземпляра, «пререндерим» поднять: при 5 это 1 142
     страницы вместо 5 834 и ~0,55 ГБ вместо 2,8. `dynamicParams = true` уже стоит,
     остальное отрисуется по первому запросу.
  2. Кэш-маунт: `RUN --mount=type=cache,target=/app/.next/cache npm run build`.
  3. Убрать `--chown` с большого `COPY`, каталог нужен только на чтение; писать —
     в отдельно созданный `.next/cache`.
  4. Дописать в `.dockerignore`: `data` (187 МБ), `logs`, `migrations`, `supabase`, `*.xlsx`.
  5. Проверить в Coolify, что слой `npm ci` кэшируется, а сборка не идёт с `--no-cache`.

  **Прежде чем делать — взять лог сборки из Coolify:** там у шагов отметки времени,
  и по ним видно, что из перечисленного реально дорого. Без лога это ранжирование
  по косвенным признакам.

- **Деплой в Coolify идёт ~10 минут — разобрано 02.09.2026, не сделано.**
  Локальная сборка при этом 105 секунд (`.next/trace-build` от 01.09): генерация статики
  55 с, очистка `.next` 25 с, копирование standalone 17 с, Turbopack 3,7 с, TypeScript 3,6 с.
  Значит дело не в компиляции.

  **База деплой НЕ тормозит** — проверено: `api.kmotors.shop` идёт не через Cloudflare
  (DNS-only на `94.16.116.251`, отвечает Kong с `x-kong-response-latency: 1ms`), внутри VPS
  запрос стоит единицы миллисекунд. Задержка в 300–900 мс — это сборка с ноутбука в прод,
  другая история. Кстати, в `.next/cache/fetch-cache` лежат оба хоста: часть сборок уходила
  в боевую базу. Там же видно цену сборки в запросах — 62 315 записей тремя порциями
  примерно по 20 800, то есть **~20 800 обращений в Supabase за сборку**, 3,5 на карточку.

  Дорого другое — то, что сборка производит:
  `.next/server/app` весит **2,8 ГБ**, из них 1,65 ГБ это 71 368 файлов `.rsc` (Next 16 пишет
  посегментные payload'ы префетча, по 12 на страницу; флага отключения в 16.2.9 в схеме
  конфига нет) и 944 МБ — 5 973 `.html`. `.next/standalone` — полная копия тех же 2,8 ГБ.
  В `Dockerfile` это уезжает строкой `COPY --chown=nextjs:nodejs`, то есть Docker переписывает
  владельца у 78 тысяч файлов одним слоем. Плюс `.next/cache` между деплоями не переносится
  вовсе: Turbopack компилирует с нуля каждый раз, fetch-кэш пустой.

  Что делать, по убыванию эффекта:
  1. **Развести два порога.** `getIndexableParts()` сейчас кормит и `generateStaticParams`,
     и карту сайта (`sitemap.ts:114` и `[slug]/page.tsx:55` зовут одну функцию), поэтому
     поднять порог в лоб нельзя — вместе со сборкой похудеет карта сайта, а это решение
     про SEO. «В индексе» оставить 2 экземпляра, «пререндерим» поднять: при 5 это 1 142
     страницы вместо 5 834 и ~0,55 ГБ вместо 2,8. `dynamicParams = true` уже стоит,
     остальное отрисуется по первому запросу.
  2. Кэш-маунт: `RUN --mount=type=cache,target=/app/.next/cache npm run build`.
  3. Убрать `--chown` с большого `COPY`, каталог нужен только на чтение; писать —
     в отдельно созданный `.next/cache`.
  4. Дописать в `.dockerignore`: `data` (187 МБ), `logs`, `migrations`, `supabase`, `*.xlsx`.
  5. Проверить в Coolify, что слой `npm ci` кэшируется, а сборка не идёт с `--no-cache`.

  **Прежде чем делать — взять лог сборки из Coolify:** там у шагов отметки времени,
  и по ним видно, что из перечисленного реально дорого. Без лога это ранжирование
  по косвенным признакам.

- **Клик по фильтру не показывает состояние загрузки — разобрано 02.09.2026, не сделано.**
  Причина замерена на чистом Next 16.2.9, а не предположена: `loading.tsx` **не срабатывает,
  когда меняются только параметры запроса**. Смена сегмента пути — скелет появляется сразу;
  смена `?brand=` — не появляется ни разу, экран стоит все 1 540 мс ответа. А на витрине
  параметрами живёт всё: сторона, расположение, сортировка, страница, марка и модель.
  Вторая причина — стримить нечему: `CatalogView` дожидается `findParts` (2 запроса),
  `getBrands` и `getModels` до того, как вернуть разметку.

  Три уровня, делать в этом порядке:
  1. `useLinkStatus` внутри `<Link>` фильтра, сортировки и пагинации — `pending` встаёт
     через ~50 мс после клика и при смене параметров тоже, сервер не участвует. Показывать
     с задержкой ~100 мс анимацией, иначе мельтешит на быстрых переходах.
  2. Закэшировать фасеты (`getBrands`, `getModels`, `getTopModels`) через `unstable_cache`:
     они не зависят от фильтра и меняются раз в скрап. Это **предусловие** к пункту 3 —
     иначе оболочка снова заблокируется на них, а прятать фильтр под скелет нельзя.
  3. Вынести `await findParts` в дочерний компонент под `<Suspense key={ключ фильтра}>`.
     **`key` обязателен**, замерено: без него fallback не показывается вовсе и коммит
     ждёт полного ответа (1 540 мс), с ним URL и скелет коммитятся за 65 мс.
  Скелет сетки уже написан в `zapchasti/loading.tsx` — выносить в общий компонент,
  а не копировать. Затрагивает все пять страниц раздела, после — прогон сборки.

- **Словарь корейских терминов реестра неполный, и закрыть его нечем.** Источник чужой
  и живой; сейчас в `lib/vin/korean.ts` 24 цвета и 13 видов топлива. Незнакомый термин
  показывается по-корейски — пополнять по мере встреч, тест на это не ругается
  и ругаться не может.
- **Папку `вин-дешифратор/` можно удалять.** Разбор и тесты из неё выделены
  в `src/lib/vin/`, остальное — скаффолд AI Studio. Из проверки типов она исключена,
  но лежит в корне и в гит пока не добавлена.
- **Не измерено, у какой доли объявлений Encar включено раскрытие.** Флаг `openData`
  у проверенного объявления стоял, но проверено ровно одно. От этой цифры зависит,
  дополнение это или основной источник страховой истории, — считать по выборке ID.
- **В `한국교통안전공단` не звонили.** У них есть `매매용 차량 이력 API` по машинам в продаже,
  по полям без согласия владельца — то есть первоисточник ровно под наш случай.
  Телефон в разделе про источники данных выше. Пока не позвонили, все разговоры
  про посредников и скрап преждевременны.
- **Ключ на `data.go.kr` не получен.** Набор `침수차량진위확인` бесплатный и согласуется
  автоматически, но нужен аккаунт на портале. До ключа подключать нечего.
- **Госномера у нас на входе нет — и это блокирует всё государственное.** Флудовый набор,
  `국토교통부` 15071233 и `첨부형 API` ищут по номеру и имени владельца, а наша форма
  принимает VIN. Связать их нечем, кроме объявления: у Encar в карточке лежат оба.
  Исключение одно — `보험개발원`: он ищет и по `차대번호`, то есть страховая история
  доступна нам напрямую по VIN.
- **Заявка на `제휴` в `보험개발원` не подана.** Это единственный источник, чей ключ поиска
  совпадает с нашим, и единственный, кто уже работает с экспортом (CARSEER, carVertical).
  Цена входа ≈ 990 000 ₩/мес за 1 000 запросов, срок 1–2 месяца, заявка только онлайн.
  До звонка в `한국교통안전공단` не подавать: если их сервис нам доступен, это дешевле.
- **Автоматизация под caranalizer не начата — решение владельца 01.09.2026 «сделаем
  в будущем».** Порядок, когда дойдут руки: (1) звонок в `한국교통안전공단`,
  (2) заявка на `제휴` в `보험개발원`, (3) роут поверх `/api/vin` теми же тремя слоями,
  что уже есть. Гейт на вход и лимит частоты переиспользуются как есть.
- **Справочника комплектаций у нас нет, и мощность взять неоткуда.** Ни один корейский
  реестр её не отдаёт — это атрибут трима, а не машины. Ближайший бесплатный вход —
  проверка Hyundai по `차대번호` (машины после января 2010); программно не проверена.
- **Хвост прежнего магазина ещё в отчёте.** 307 755 адресов «обнаружена,
  не проиндексирована». Сделано всё, что от нас зависит: карта сайта отправлена
  26.08.2026, а сами адреса с того же дня отвечают 410 вместо 301 на kmotors —
  редирект вёл на страницу под `noindex` и не склеивал ничего. Дальше только ждать:
  инструмента, который бы вычистил такой хвост, не существует. Проверять раз в месяц,
  падает ли число — ближайший разумный срок конец сентября.
- **Оферту должен посмотреть юрист** — обе версии. Ограничение возврата держится на том,
  что деталь выкупается под конкретную заявку; довод от страны не зависит, а право
  потребителя зависит. В английской версии есть оговорка про обязательные нормы страны
  покупателя — её тоже проверить.
- **В оферте не хватает фактов, которых у нас нет:** срок на осмотр после получения,
  кто платит обратную пересылку, порядок при повреждении в пути, лимиты беспошлинного
  ввоза, реквизиты юрлица. Это не перевод, а данные от владельца. Список — в `AGENTS.md`.
- **Согласие на обработку данных есть только в корзине.** У формы проверки и обратной
  связи его нет, хотя они тоже собирают имя и телефон. Разбирать вместе с юристом.
- **Шапка на иноязычных страницах ведёт на редиректы.** `/en/about`, `/en/faq`
  и `/en/how-it-works` открыты, а вот арабские — нет, и ссылки на них в шапке остаются
  редиректом на страницу проверки. Чинить в навигации, а не в middleware.
- **Электронная коммерция Метрики пустая.** В конфиге объявлена, но нужен её формат
  в `dataLayer`, а мы шлём события gtag. Без цены заводить второй формат смысла мало.
- **Почты в формах заявок по-прежнему нет.** Регистрация ловит адрес только у тех, кто
  завёл аккаунт, а это единицы процентов от потока. Поле email в корзине, форме проверки
  и контактах плюс колонка в `leads` поймали бы адрес у всех, кто оставляет заявку, —
  работа на час, и она отложена сознательно, а не забыта.
- **Заявки к аккаунту не привязаны.** В общей с kmotors таблице `leads` колонки
  пользователя нет, и заводить её в чужой таблице ради одного экрана незачем. Пока
  кабинет показывает только контакты, без истории заказов.
- **Письма пока попадают в спам.** Домен подтверждён и DMARC добавлен только 30.08.2026,
  репутации у отправителя ещё нет. Свой шаблон вместо стандартного уже помогает, дальше
  нужно время и отметки «не спам» на первых письмах. Проверить через неделю.

## Прод и деплой: порядок, который обратного не терпит

**Код и база связаны.** Витрина читает `partsfit_*`; выкатить код на сервер раньше,
чем в боевой базе появятся таблицы, — значит положить раздел запчастей целиком.
Поэтому порядок такой:

```
1. миграции на VPS   supabase/migrations/*.sql через SQL Editor, по порядку имён
2. проверка          npm run partsfit:verify -- --prod
3. заливка           npm run partsfit:load   -- --prod
4. проверка ещё раз  npm run partsfit:verify -- --prod
5. и только теперь   деплой кода
```

Шаг 2 до заливки — не перестраховка: на локальном стеке мы уже ловили, что таблицы
из миграции не получают даже права на чтение, и на проде это выяснилось бы после того,
как 314 тысяч строк уже уехали. На пустой базе проверка сама переходит в режим «только
схема, права, ограничения и RLS» — содержательные проверки пропускаются с пометкой,
а не валятся кучей.

**Боевая база общая с kmotors** — 42 чужие таблицы в той же схеме. Поэтому только
SQL Editor: `supabase db push` и `supabase db reset` на прод не запускать никогда
(`reset` снёс бы kmotors целиком). Подробности и ловушка `parts_*` против `partsfit_*` —
в `AGENTS.md`.

Порядок проверен репетицией на локальной базе целиком: `db:reset` → `verify` (пустая,
схема и права зелёные) → `load` (10 с) → `verify` (35 проверок зелёные).

**Пройден на проде 26.08.2026 целиком, включая шаг 5.** Три миграции через
SQL Editor, проверка по пустой базе (схема, права, RLS), заливка 313 925 строк,
проверка ещё раз — 35 из 35. Расхождений
с локальным стеком не оказалось ни одного: гранты легли, ограничения ловят, аноним
читает все девять объектов и не пишет. Времена запросов с ноутбука — 300–900 мс против
2–12 мс локально; это круговая задержка до VPS, а не база: сайт живёт на том же хосте
и по сети не ходит. Проверить настоящие времена — на собранном проде.

Сборка перед выкаткой прошла без единой ошибки: 5 860 пререндеренных маршрутов,
из них 5 834 карточки, динамическими остались ровно четыре листинговых маршрута
и три API. Счётчик «Generating static pages» показывает при этом 8 035 — это его
внутренние единицы, а не маршруты; сверять надо по `.next/prerender-manifest.json`,
иначе расхождение читается как поломка.

**Что нужно на сервере.** Те же переменные, что и раньше, ничего нового:
`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`.

Оговорка про ключи: `createServerClient()` в `lib/supabase.ts` берёт `service_role`,
если он задан, и только иначе анонимный.

**30.08.2026 выяснилось, что на проде он не задан вовсе** — сайт всё это время работал
анонимным ключом. Не проявлялось потому, что каталог анониму открыт, а `leads` —
таблица общая с kmotors и тоже открыта. Сломалось на первой нашей таблице, закрытой
от анонима: кабинет отвечал «не удалось сохранить», и причина не была видна ниоткуда.
Значит, и обратное неверно: RLS на сервере **сработает**, и закрытая политикой часть
каталога ослепит витрину. Роут `/api/auth/sync` теперь требует ключ явно и падает
при загрузке модуля с именем переменной в сообщении.

**Чего на сервере быть не должно:** `.env.local`. Он локальный и перекрывает `.env` —
уехав на сервер, увёл бы прод на `127.0.0.1:54321`.

**Миграции** (порядок по именам файлов):

| файл | что создаёт |
|---|---|
| `20260825000000_partsfit_catalog.sql` | таблицы каталога, права, RLS |
| `20260825120000_partsfit_fitment.sql` | таблица совместимости |
| `20260825160000_partsfit_facets.sql` | представления счётчиков для фильтра |
| `20260829000000_skywheel_wheels.sql` | таблицы дисков со skywheel.kr |
| `20260829140000_partsfit_customers.sql` | зарегистрированные покупатели |
| `20260830120000_partsfit_vin_lookups.sql` | журнал проверок по VIN и лимит частоты |

## Локальный стек между сессиями

```bash
npm run db:stop     # данные в томе Docker остаются
npm run db:start    # поднять обратно
```

`supabase stop` без `--no-backup` том сохраняет. Если база всё же потеряется,
восстановление — две команды: `npm run db:reset && npm run partsfit:load`, минута.

## Как посмотреть данные

```bash
npm run partsfit:viewer      # http://localhost:4123
```

Локальный просмотр собранного: поиск по артикулу и марке, фильтры по типу детали,
карточка детали со списком экземпляров — цена, состояние, разъём, цвет, VIN донора,
фотографии и исходное описание по-корейски. Инструмент разработчика, к сайту отношения
не имеет; заведён в `.claude/launch.json` как `partsfit-viewer`.

## Локальная база

```bash
npm run db:start        # поднять стек (Studio на http://127.0.0.1:54323)
npm run db:reset        # пересобрать базу с нуля из миграций
npm run partsfit:load   # залить каталог — 314 тыс. строк за ~11 секунд
npm run partsfit:verify # 24 проверки: ограничения, права, запросы, содержимое
npm run db:stop
```

**Всё по умолчанию идёт в локальную базу.** В `.env` лежат боевые ключи, в `.env.local` —
локальные, и второй перекрывает первый. Сверх того `scripts/partsfit/target.mjs` проверяет
адрес и на всё, что не localhost, требует явный `--prod`. Ошибиться молча нельзя.

Проверка не просто считает строки — она намеренно делает то, что **обязано упасть**:
дубль адреса, экземпляр без детали, запись анонимным ключом. Плюс замеряет запросы
витрины и сверяет содержимое. Если что-то отвалится на проде, оно отвалится и здесь.

## Что сделано в интерфейсе

Магазин на сайте работал целиком и до переезда: 10 страниц, 15 компонентов, корзина,
оформление заказа, фильтры, поиск, галерея, цены по курсу ЦБ. Его не переписывали —
переподключили к новым данным и добавили то, чего в старой модели не было.

**Шаг 1. Типы и слой доступа.** `src/types/part.ts` разводит деталь и экземпляр,
`src/lib/shop/catalog.ts` читает Supabase теми же двенадцатью функциями — компоненты
не тронуты. Счётчики фильтра считает база представлениями `partsfit_brand_counts`
и `partsfit_model_counts`.

**Шаг 2. Категории и тексты.** Одиннадцать типов деталей в реестре `CATEGORIES`, пилюли
переехали под заголовок и переносятся на вторую строку, тексты раздела и сайта больше
не обещают одну оптику. Хлебные крошки появились на категории, марке и модели — до этого
их там не было вовсе.

**Шаг 3. Карточка со списком экземпляров.** `components/shop/offer-picker.tsx`:
на детали с 64 экземплярами покупатель выбирает конкретный, и вместе с выбором
переключаются цена, галерея, состояние, разъём, цвет и VIN. В корзину уезжает выбранный
экземпляр, а не деталь.

**Шаг 4. Страница артикула.** `/zapchasti/oem/<номер>` — на каких машинах встречается
номер и что есть в наличии, с поиском и по кросс-номерам.

**Главная.** Вход в каталог: поиск по артикулу в герое, типы деталей, свежие поступления,
марки. Проверка по VIN осталась блоками ниже.

**Аналитика раздела — с 28.08.2026.** Счётчики (GA4, Метрика, Clarity) стояли давно,
но из магазина не уходило ни одного события: был виден трафик и не был виден путь.
Теперь идут четыре стандартных события GA4 — `view_item`, `add_to_cart`,
`begin_checkout`, `generate_lead`. Цена в них не передаётся, это решение владельца.
Значения `lead_source` совпадают с `source_page` в таблице `leads`, и сходиться они
должны до штуки — это единственный способ проверить, что счётчик не врёт.

**Шаг 5. Заливка в прод — сделан 26.08.2026.** Порядок и подробности выше, в разделе
про деплой. Схема с тех пор не менялась: правки 28.08 кода не касаются базы вовсе,
и деплой у них в один шаг.

**Английский, доллар и новая форма — 28.08.2026.** Раздел двуязычен целиком, включая
корзину и оферту; переключатель языка `LocaleToggle` стоит в шапке везде, кроме страницы
проверки по VIN — там свой, на три языка.

## Сколько страниц публиковать

Всего в базе получается **18 655 карточек** плюс 2 180 посадочных. Это перебор по двум
причинам сразу, и обе надо развести:

**Сборка.** До переезда было 994 страницы статикой за 9 секунд. ISR в Next 16 есть
(`node_modules/next/dist/docs/01-app/02-guides/incremental-static-regeneration.md`):
`generateStaticParams` отдаёт подмножество, остальное рендерится по первому запросу
и кэшируется. Оговорки оттуда: нужен Node-рантайм (у нас `output: standalone` — есть),
файловый кэш свой у каждого инстанса, наблюдать через заголовок `x-nextjs-cache`.
Вышло 5 860 страниц за 2,5–5 минут: пререндерятся посадочные и карточки с двумя
и более экземплярами, остальные рендерятся по первому запросу.

**Индексация.** В Search Console 307 755 адресов «обнаружена, не проиндексирована».
Добавить туда 18 тысяч — значит утопить их в том же хвосте.

И главное: **12 821 карточка (69%) держится на одном экземпляре**. Продадут — страница
умрёт. Публиковать их в карту сайта значит заранее наплодить 404.

| экземпляров на странице | страниц |
|---|---|
| 1 — живёт до первой продажи | 12 821 |
| 2–4 | 4 692 |
| 5–9 | 836 |
| 10 и больше | 306 |

**Решено 25.08.2026, уточнено 28.08.2026:**

| что | сколько | в индекс |
|---|---|---|
| посадочные (категории, марки, модели) | 2 180 | да, пререндер |
| карточки с 2+ экземплярами | 5 834 | да, пререндер |
| карточки с одним экземпляром | 12 821 | **`noindex`**, вне карты сайта |
| страницы артикулов с совместимостью | 15 708 | **да, с 28.08.2026** |
| страницы артикулов без совместимости | 1 047 | **`noindex`**, вне карты сайта |

Карточка с одним экземпляром страницу всё равно получает — иначе товар нельзя положить
в корзину. Но `robots: { index: false }` в `generateMetadata` и отсутствие в `sitemap.ts`:
продалась — исчезла тихо, не оставив 404 в индексе. Признак — `offers_count = 1`.

**Страницы артикулов ждали расчистки индекса и не дождались:** решение владельца
от 28.08.2026 — ждать хвост можно бесконечно, а эти страницы самые ценные по трафику.
Опубликованы не все: у 1 047 артикулов нет строки в `partsfit_fitment` (у донора
расходится тип детали или сторона), показать «встречается на этих машинах» нечем,
и страница держится только на товаре — пропадёт товар, останется 404. То же правило,
что и у карточек с одним экземпляром. Список для карты сайта даёт
`getIndexableOemNumbers()` — он читает таблицу совместимости, то есть только выживших.

**Английские страницы в пререндер не идут.** `generateStaticParams` дочерних маршрутов
по-прежнему возвращает пустой список для не-`ru`: сборка осталась 5 860 маршрутов,
английское рендерится по первому запросу. Трафика на них пока нет, а пререндер
удвоил бы сборку.

## Отменено по ходу

- Удаление 472 записей без артикула из `catalog.json` и склейка `catalog-front.json`
  с `catalog-rear.json` (файлы в `~/Desktop/архив/`). Обе группы перекачиваются заново
  и с данными лучшего качества — чинить старые файлы незачем.
- `catalog-rear.json` из архива совпадал поле в поле с тогдашним `src/data/catalog.json`
  — это была не резервная копия чего-то другого, а тот же каталог. Сам файл с тех пор
  удалён вместе с `raw_parts.json` и `normalize-parts.mjs`.

## Команды

```bash
npm run partsfit:index    -- <группа>          # индекс по листингам
npm run partsfit:coverage -- <группа> --oem    # чего не хватает в словаре
npm run partsfit:details  -- <группа>          # карточки отобранного
npm run partsfit:tables   -- <группа> [ещё...] # таблицы под Supabase
npm run partsfit:terms                         # полнота английского словаря
npm run partsfit:load     -- --dry-run         # посчитать, ничего не отправляя
npm run partsfit:viewer                        # локальный просмотр данных

npm run db:start / db:reset / db:stop          # локальный Supabase
npm run partsfit:load     [-- --prod]          # заливка в базу
npm run partsfit:verify   [-- --prod]          # 35 проверок базы
```

`partsfit:terms` гонять после каждого `partsfit:tables`: новая группа донора приносит
новые термины, и без проверки они молча уедут на английские страницы по-русски —
сборка не упадёт, глазами это ловится только случайно.

Группы: `headlights`, `taillights`, `control-units`, `side-mirrors`, `bumpers`, `doors`,
`fenders`, `sensors`, `wheels`, `misc`, `donor_cars`. Список — в `scripts/partsfit/lib.mjs`.

## Данные на диске

```
data/partsfit/index/    17 МБ  под контролем версий — в нём копится firstSeen
data/partsfit/details/  46 МБ  игнорируется, пересобирается скрапом
data/partsfit/tables/   83 МБ  игнорируется, выводится из первых двух за секунды
```
