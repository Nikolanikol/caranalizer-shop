-- Схема переехала в supabase/migrations/.
--
-- Держать её в двух местах нельзя: копии разъезжаются молча, и однажды локальная база
-- перестаёт совпадать с продакшеном — ровно та беда, ради которой заводили локальный
-- Supabase. Единственный источник теперь один:
--
--   supabase/migrations/20260825000000_partsfit_catalog.sql
--
-- Менять схему: создать новую миграцию (`supabase migration new <имя>`), а не править
-- старую — применённую миграцию Supabase считает неизменной и повторно не выполняет.
--
-- Применить локально:  supabase db reset      (пересобирает базу с нуля)
-- Применить в проде:   supabase db push
